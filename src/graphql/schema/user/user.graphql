type User {
  userId: Int!
  portalId: String!
  password: String!
  nickname: String!
  randomNickname: String!
  registeredAt: String!
  posts: [Post]
}

type Query {
  users: [User!]!
  user(userId: Int): User
}
