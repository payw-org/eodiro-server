type User {
  id: Int!
  portalId: String!
  password: String!
  nickname: String!
  randomNickname: String!
  registeredAt: String!
  posts: [Post]
}

type Query {
  users: [User!]!
  user(id: Int): User
}
