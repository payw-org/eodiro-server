type Post {
  id: Int!
  boardId: Int!
  title: String!
  body: String!
  userId: Int!
  randomNickname: String!
  uploadedAt: String!
  editedAt: String!
}

type Query {
  post(id: Int): Post
  posts: [Post]!
}
